# Planned logging configuration; runtime loader is not implemented.
version: 1
disable_existing_loggers: false
handlers:
  console:
    class: logging.StreamHandler
    level: DEBUG
root:
  level: INFO
  handlers: [console]
